apiVersion: v1
kind: Service
metadata:
  name: static-website-svc
  namespace: portfolio
  labels:
    app: static-website
spec:
  type: ClusterIP
  selector:
    app: static-website
  ports:
  - name: http
    port: 80
    targetPort: http