apiVersion: v1
kind: Service
metadata:
  name: static-website-svc
  namespace: portfolio
  labels:
    app.kubernetes.io/name: portfolio
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: portfolio